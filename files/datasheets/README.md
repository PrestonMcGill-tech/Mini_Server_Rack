# Datasheets

Drop manufacturer datasheets/manuals for key components here — rack rails, PDU, fans,
casters, whatever's load-bearing or worth referencing later (e.g. when you're
troubleshooting or planning the next upgrade).

Keep filenames matching the part, e.g. `pdu-model-xyz-datasheet.pdf`.
